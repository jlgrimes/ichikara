# Ichikara — environment variables template
# Copy to .env (dev) or .env.production (prod), then fill in real values.
# NEVER commit .env or .env.production to git.

# ── Supabase ──────────────────────────────────────────────────────────────────
# IMPORTANT: Use the anon/public key here (NOT service_role).
# The service_role key bypasses RLS — never expose it in the client app.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Monitoring (optional) ─────────────────────────────────────────────────────
# Highlight.io — session replay + error tracking
# Create project at app.highlight.io
VITE_HIGHLIGHT_PROJECT_ID=

# ── Analytics (optional) ──────────────────────────────────────────────────────
# PostHog — product analytics
# Create project at app.posthog.com
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com

# ── App ───────────────────────────────────────────────────────────────────────
VITE_APP_VERSION=0.1.0

# ── Dev only ──────────────────────────────────────────────────────────────────
# Set to 'true' to bypass Supabase auth in local dev (stub user only)
# VITE_DEV_MOCK_AUTH=false
